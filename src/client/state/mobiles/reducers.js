import * as types from './actionTypes';
import { handleActions } from 'redux-actions';
import update from 'immutability-helper';

/*update.extend('$updateMobile', (mobile, original) => {

});*/
export default handleActions({
}, {
    mobiles: []
});
